# FRED(세인트루이스 연준) API 키 — 홈 탭 GDP 지표 수집에 사용
# 무료 발급: https://fred.stlouisfed.org/docs/api/api_key.html
# 로컬 테스트 시 .env로 복사해 값을 채우거나 직접 export 하세요.
# GitHub Actions에서는 저장소 Settings → Secrets and variables → Actions 에
# 동일한 이름(FRED_API_KEY)으로 등록해야 합니다.
FRED_API_KEY=
